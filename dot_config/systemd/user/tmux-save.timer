[Unit]
Description=Periodic tmux session save

[Timer]
# tmux-persist has no periodic save of its own; its hooks only cover detach and
# session-close. This bounds crash/poweroff loss to the interval below.
OnBootSec=5min
OnUnitActiveSec=5min
AccuracySec=30s

[Install]
WantedBy=timers.target
